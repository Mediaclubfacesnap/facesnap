# JWT Validation Report (Phase 4)

This report details the structure, claims, and validation of the JWT token generated by the `/sync-oauth` endpoint.

## Token Generation Function (from app/routes/auth.py)
```python
def create_access_token(data: dict) -> str:
    to_encode = data.copy()
    now = datetime.datetime.utcnow()
    expire = now + datetime.timedelta(minutes=settings.ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode.update({"exp": expire, "iat": now})
    return jwt.encode(to_encode, settings.JWT_SECRET, algorithm=settings.JWT_ALGORITHM)
```

## Decoded JWT Payload
```json
{
  "sub": "00000000-0000-0000-0000-000000000000",
  "jti": "a40fd34a-c98f-448f-bbc1-a8a54caa4b09",
  "exp": 1780998751,
  "iat": 1780912351
}
```

## Claims Verification
* `sub` (Subject / User ID): **Verified** (`00000000-0000-0000-0000-000000000000`)
* `jti` (JWT ID / Session identifier): **Verified** (`a40fd34a-c98f-448f-bbc1-a8a54caa4b09`)
* `exp` (Expiration time): **Verified** (`1780998751`)
* `iat` (Issued at time): **Verified** (`1780912351`)

## Expiration Verification
* Expiration Duration: `1440.0` minutes (24 hours).
* Configuration setting: `ACCESS_TOKEN_EXPIRE_MINUTES = 1440` (matches perfectly).
